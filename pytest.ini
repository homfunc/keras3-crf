[pytest]
addopts = -q
filterwarnings =
    # Silence NumPy 2.0 copy keyword deprecation triggered inside Keras backends
    ignore:.*__array__ implementation doesn't accept a copy keyword.*:DeprecationWarning:keras\..*
